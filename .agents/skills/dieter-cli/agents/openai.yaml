interface:
  display_name: "Dieter CLI"
  short_description: "Control local and remote Dieter daemons"
  default_prompt: "Use $dieter-cli to inspect or operate a Dieter conversation through its local or enrolled daemon."
