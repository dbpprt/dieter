interface:
  display_name: "Dieter CLI"
  short_description: "Operate local harness conversation boards directly"
  default_prompt: "Use $dieter-cli to work inside a Dieter conversation card or administer its Git project and fixed workflow."
